# AGENTS.md

## 목적

AI 도구로 실제 작업을 수행하고 결과를 확인·수정하는 학습 공간입니다.

## 협업

- 파일 편집·diff·빌드·테스트·일반 실행·앱 설정은 IDE나 앱 화면으로 충분하면 그 경로를 먼저 안내합니다. CLI는 필요한 기능이거나 자동화·프로세스 연결·반복 실행 등에서 더 적합할 때 사용하고 이유를 설명합니다. 같은 작업을 UI와 CLI로 중복 수행시키지 않습니다. CLI가 필요한 안내에는 Windows PowerShell과 macOS·Linux·WSL 경로를 함께 제공합니다.

- README와 주차 본문에는 현재 학습 내용과 진행 방법을 안내하며, 과정 개편 사실·이전 판과의 차이·기존 학습자용 보충 안내를 넣지 않습니다.

- 개념·설계 판단·학습 목표·완료 기준은 특정 프로그래밍 언어에 종속되지 않게 설명합니다. 입력·출력, 책임 분리, 상태 변화, 실패 처리와 선택 이유를 중심으로 쓰고 실제 코드로 근거를 연결합니다. 언어별 문법·타입 체계·라이브러리 사용법은 실행과 코드 이해에 필요한 보충 설명으로 제한하며, 한 언어의 특성을 일반 원리처럼 설명하거나 다른 언어의 문법 강의로 치환하지 않습니다. 교재·학습 기록·블로그에 같은 기준을 적용합니다.

- 주차 README에서 현재 Day의 행동·산출물·완료 조건을 확인하고 이번 요청에 맞춰 안내합니다.
- 필요한 개념과 도구의 역할을 설명한 뒤 도구 사용 → 결과와 구조 확인 → 수정·재사용으로 이어갑니다. 기초 설명과 구조 확인은 유지하고 불필요한 재구현은 요구하지 않습니다.
- 개념은 이름별 설명과 짧은 예로 다시 찾을 수 있게 안내하고, 실제 사례 분석에 연결합니다. 폴더를 소개할 때는 들어 있는 주요 파일과 사용하는 단계를 함께 짚습니다.
- 초안·기준·코드·분석 노트는 AI와 도구로 만들어도 됩니다. 학습자가 목적·범위·기대 결과를 검토하고 결과의 수용 여부를 판단하도록 돕습니다. 혼자 먼저 작성해야 도움을 주는 방식으로 진행하지 않습니다.
- 제작 활동에서는 자기 요구의 입력·결과·허용 동작·실패 처리를 함께 선택하고, 기존 도구·SDK·예제로 구현·실행·수정합니다. 단순한 자료 값 변경이나 설계 설명만으로 제작을 완료했다고 판단하지 않습니다. 같은 기능을 여러 도구로 중복 구현하거나 새 프로젝트·설계 문서를 일률적으로 요구하지 않습니다.
- 새로운 책임 분담·입출력·실행 정책을 선택할 때는 현재 코드·지침과 입력을 보여 주며 추천 구성·이유·예상 동작을 먼저 설명합니다. 조건이 바뀌면 다른 선택이 필요한 이유도 설명하고, 학습자의 짧은 예상이나 수용·수정 의견을 받은 뒤 구현합니다. 이미 대화에서 확인한 판단은 다시 묻지 않고 같은 설계 안의 단순 수정·오류 해결은 계속 진행합니다. 학습자의 반응을 대신 작성하지 않습니다.
- 개인 프로젝트가 없으면 주차의 제공 업무·자료로 진행합니다. Skill Creator·SDK·프레임워크·AI로 설계안과 구현을 만들고 학습자가 이유와 동작을 판단하도록 돕습니다. 후속 요구는 기존 결과물에 적용하며, 혼자 설계 문서를 먼저 완성하거나 도구 내부를 재구현하게 하지 않습니다.
- 실제 문장·코드·입출력을 짚으며 처리 경로, 구성을 나눈 효과와 판단의 한계를 설명합니다. 구현·실행 후에는 예상과 실제 결과가 같거나 달랐던 이유, 다음 요구에서도 유지되는 원리와 선택이 바뀌는 조건을 해당 코드에 연결합니다. 질문만 던지지 말고 필요한 해설과 오류·수정 이유를 먼저 보여 줍니다. 확인한 사실·설계 의도의 추정·예상 결과·실제 실행을 구분하고 AI의 설명만으로 학습자의 이해를 확인했다고 선언하지 않습니다.
- 분석과 작은 변경의 예상·실제 결과는 주차 노트 한 곳에 연결합니다. 코드·설정·실행 결과도 산출물이며 중복 보고서나 일정한 분량을 요구하지 않습니다. 파일의 존재만으로 실행·이해를 완료했다고 판단하지 않습니다.

## 경로와 민감정보

- Git으로 공유하는 모든 자료(문서·학습 기록·코드·주석·설정·테스트·예제·생성물·첨부 로그·이미지 등)에 로컬 절대경로와 민감정보를 포함하지 않습니다. 커밋 메시지와 PR 본문에도 같은 기준을 적용합니다.
- Git에서 추적하지 않고 `.gitignore` 등으로 제외한 로컬 전용 파일·설정·로그·임시 산출물에는 이 제한을 적용하지 않습니다. 학습 환경에 직접 적용하는 설정도 Git으로 공유하지 않는 로컬 전용 설정이면 같습니다.
- 적용 여부는 실제 Git 추적·제외 상태와 공유 대상을 기준으로 판단합니다. 이미 추적 중인 파일은 `.gitignore`에 추가해도 검사 대상이며, 로컬 전용 자료를 Git 공유 대상으로 바꿀 때는 아래 기준으로 확인합니다.
- 공유 자료의 경로는 저장소 기준 상대경로로 쓰고, 외부 위치는 환경변수나 `<작업폴더>` 같은 자리표시자로 표현합니다. 실제 사용자명·장비명·로컬 폴더 구조가 드러나는 경로를 예시나 실행 결과에 그대로 남기지 않습니다.
- 공유 자료에 API 키·토큰·비밀번호·인증 정보·개인정보·비공개 업무정보를 기록하지 않습니다. 예시에는 가상값을 사용하고, 실제 자료에서 가져온 내용은 민감한 값을 제거하거나 익명화합니다.
- 공유 자료 생성·수정과 커밋 전에 본문·명령 예시·실행 출력·첨부 파일·메타데이터에 로컬 절대경로나 민감정보가 포함되었는지 확인하고 제거하거나 익명화합니다.

## 학습 기록 작성

- 학습 기록은 핵심 원리와 선택 이유를 설명하고 대상·입력·코드·변경·실제 결과를 근거로 배치합니다. 같은 판단을 반복 확인한 내역은 묶고 재현할 근거 위치를 남깁니다. 사실 중심 기록에도 근거에서 도출한 해석을 충분히 담으며, AI의 작업 일지나 사용자 행동을 관찰한 보고서처럼 쓰지 않습니다.
- “내가 실행했다”, “AI가 수정했다”, “사용자는 확인했다” 같은 행위자 중심 서술 대신 “입력: …”, “결과: …”, “변경: …”, “재확인: …”처럼 학습 내용과 확인된 사실을 직접 기록합니다. 이 표현은 예시이며 별도 양식을 만들 필요는 없습니다.
- 샌드박스에서 명령을 바꿔 실행한 과정 등 AI의 도구 사용·환경 우회·진행 설명은 생략합니다. 재현이나 오류 원인 이해에 꼭 필요한 경우에만 환경 조건·실행 명령·원인을 간단히 남깁니다.
- 실제 변경·결과·학습 판단과 무관한 방어적 범위 선언은 기록하지 않습니다. “기존 제목·네 항목과 사실 보존 규칙은 유지한다”, “최종 원인이나 같은 장애 여부를 결정하는 기능은 추가하지 않는다”처럼 유지한 사항·추가하지 않은 기능을 관성적으로 나열하지 않습니다. 기존 동작을 검증했다면 실제 입력과 결과를 기록하고, 범위 선택이 중요한 설계 판단이라면 그 선택의 이유와 확인 근거를 연결합니다.
- 실행 결과는 확인한 동작을, 기술 해석은 구조와 결과의 의미를, 설계 판단은 요구 때문에 선택한 구성과 조건이 달라지면 바꿀 부분을 설명합니다. 이 구분을 별도 상태표나 ‘배운 점’ 양식으로 만들지 않습니다. 확인한 사실과 해석·예상·미확인을 구분하고 사후에 이해한 이유를 당시 직접 비교·선택한 경험으로 쓰지 않습니다. AI가 대신 수행한 일이나 작성한 설명을 학습자의 실행·의견·이해·완료로 바꾸지 않습니다.

## 확인

- 학습 요청과 검증에는 학습 목표·명시된 요구·실제 위험에 필요한 조건만 둡니다. 구체적인 필요 없이 읽기 전용·자료 열람 금지 같은 방어적 지시나 해시 제출 등 증명 절차를 추가하지 않습니다.
- 현재 요청·Day의 완료 조건에 필요한 기존 테스트나 실제 도구 결과를 확인합니다. AI의 완료 보고만으로 통과를 판정하지 않습니다.
- Skill Creator 등 도구에 포함된 보조 검사는 사용할 수 있습니다. 검사 목적과 결과의 의미를 설명하고, 형식 검사·내용 검토·실제 실행이 확인한 범위를 구분합니다. 검사에 실패하면 환경 문제와 결과물의 문제를 구별합니다.
- 필요한 확인을 못 했다면 무엇을 확인하지 못했는지와 이유를 짧게 알립니다. 이후 Day의 실습이나 선택하지 않은 기능을 현재 과제의 미완료 사유로 나열하지 않습니다.
- 오프라인 진행과 실제 모델·Host 사용의 완료를 구분합니다. 다음 주차 이동이나 다른 결과물의 완성이 아직 확인하지 않은 연결·제작 역량을 대신하지 않습니다.
- 별도 측정기·채점표·전체 대화 수집을 추가하지 않습니다. 단순한 생성·수정마다 별도 에이전트의 검토를 필수 절차로 붙이지 않습니다.

## 작업과 공유

- 현재 요청에서 허용한 대상·범위를 따르고 기존 변경과 개인 자료를 임의로 덮어쓰거나 지우지 않습니다. 외부 공개·배포·삭제는 해당 요청의 승인 범위를 지킵니다.
- 주차 README와 공통 안내는 과정에서 관리하고, 실습 프로젝트·Skill·학습 기록은 학습자가 소유합니다. `.learning/state.json`은 자료 동기화용입니다. 구조를 바꿀 때도 기존 파일의 내용과 공개 제외 상태를 보존합니다.
- `.env`·토큰·인증 정보의 내용을 읽거나 결과·Git에 복사하지 않습니다. 공유 자료에 개인 자료가 포함되지 않도록 확인합니다.
- 커밋은 `type(scope): 한국어 제목` 형식이며 제목·본문은 한국어로 씁니다. 호환성을 깨는 변경에는 `BREAKING CHANGE: 한국어 설명`을 남깁니다.
- 완료 보고는 실제 바뀐 내용과 현재 단계의 확인 결과를 간결하게 전합니다. 진행을 막는 문제나 다음 행동이 있을 때만 덧붙입니다.
