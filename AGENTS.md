# AGENTS.md

## 저장소 목적

이 저장소는 AI를 활용한 개발 과정을 직접 실험하고, 그 결과를 재현 가능한 기록으로 남기기 위한 학습 공간입니다.

## 작업 범위

- 요청에서 허용한 경로만 수정합니다.
- 기존 변경과 실험 로그를 임의로 덮어쓰거나 지우지 않습니다.
- 저장소 밖의 파일은 수정하지 않습니다.
- Git 커밋 메시지는 AngularJS 커밋 컨벤션의 `type(scope): 제목` 형식을 따릅니다.
- `type`과 `scope`는 `feat(course)`, `fix(runner)`, `docs(week01)`, `test(week01)`, `chore(repo)`처럼 짧은 영문으로 쓰고, 제목과 본문은 한국어로 작성합니다.
- 호환성을 깨뜨리는 변경은 본문이나 footer에 `BREAKING CHANGE: 한국어 설명`으로 표시합니다.

## 검증

- 요청에 적힌 검증 명령을 실행합니다.
- 검증하지 못한 항목은 통과했다고 추정하지 않고 `NOT_VERIFIED`로 보고합니다.

## 보안과 비밀값

- `.env`, 토큰, 인증 정보의 내용을 읽거나 결과에 복사하지 않습니다.
- 공개할 로그에는 개인 경로와 비밀값이 남지 않도록 확인합니다.

## 완료 보고

변경한 파일, 실행한 검증, 검증 결과와 남은 위험을 짧게 나눠 보고합니다.
