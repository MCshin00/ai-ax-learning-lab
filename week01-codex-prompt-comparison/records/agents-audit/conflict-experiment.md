# AGENTS.md 안전한 충돌 실험

## 상태

- 실행 완료
- 임시 충돌 규칙 제거 완료

## 실험 대상

- 루트 `AGENTS.md`: Git 커밋 제목과 본문을 한국어로 작성
- 하위 `lab/ticket-title-normalizer/AGENTS.md`: Git 커밋 제목과 본문을 영어로 작성
- 실제 Git 커밋은 만들지 않음

## 실행 전 예상

- 현재 작업 폴더에 더 가까운 하위 규칙이 우선 적용되기 때문에 Git 커밋 제목과 본문은 영어로 작성될 것이다.
- 하위 규칙과 충돌하지 않는 루트 규칙은 계속 적용될 것이다.

## 확인할 항목

- Codex가 한국어와 영어 규칙의 충돌을 찾는가?
- 어느 파일의 규칙이 우선한다고 설명하는가?
- 충돌하지 않는 AngularJS 커밋 형식과 영문 `type`·`scope` 규칙이 계속 적용된다고 설명하는가?
- 저장소를 수정하지 않는가?

## 실제 결과

- Codex는 루트와 현재 디렉터리의 `AGENTS.md`를 모두 찾았다.
- Git 커밋 제목과 본문의 한국어·영어 규칙이 같은 대상을 지시하는 명확한 충돌이라고 판단했다.
- 현재 디렉터리에 더 가까운 하위 규칙이 우선하므로 제목과 본문은 영어로 작성해야 한다고 설명했다.
- 충돌하지 않는 루트의 `type(scope): 제목` 형식과 영문 `type`·`scope` 규칙은 계속 적용된다고 설명했다.
- 완료 보고 규칙은 직접 충돌하지 않으므로 루트의 더 상세한 항목까지 포함하는 것이 안전하다고 설명했다.
- 두 `AGENTS.md`를 찾고 읽는 명령만 실행했으며 저장소를 수정하지 않았다.

## 예상과 실제 비교

- 예상과 실제가 일치했다.
- 가까운 하위 파일이 충돌하는 언어 규칙만 대체했다.
- 충돌하지 않는 루트 규칙은 계속 적용됐다.

## 근거

- 모델·reasoning: `gpt-5.6-terra` / `medium`
- sandbox: `read-only`
- 실제 프롬프트 전송: 2026-08-05 22:59:56 KST
- 최종 응답: 2026-08-05 23:00:31 KST
- 도구 호출: `AGENTS.md` 탐색 및 원문 읽기 2회
- 쓰기 도구 호출: 없음

## 정리

- 하위 `AGENTS.md`의 임시 `Git 커밋` 섹션을 제거해 원래 규칙으로 복구했다.
