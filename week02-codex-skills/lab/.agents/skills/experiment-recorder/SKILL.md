---
name: experiment-recorder
description: TODO — raw evidence가 존재한 뒤 run summary가 필요할 때만 선택되도록 작성한다.
---

# Experiment Recorder

TODO:

- 환경·시작 commit·하네스 설정·프롬프트·Codex JSONL·테스트·시간·diff를 수집한다.
- `Q_first`와 `Q_final`을 분리한다.
- 측정값과 해석을 분리한다.
- 누락값은 추측하지 않고 `NOT_AVAILABLE`로 기록한다.
- `run-summary.json`, `run-summary.md`, `blog-evidence.md`를 만든다.
